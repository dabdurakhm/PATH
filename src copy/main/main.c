#include "pathfinder.h"

int main (int argc, char *argv[]) {
     t_islands *isl = (t_islands *)malloc(sizeof(t_islands));
     mx_error_hangling(argc, argv[1], isl);
     // system ("leaks -q pathfinder");
}
