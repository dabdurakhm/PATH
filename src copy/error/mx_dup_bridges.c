#include "pathfinder.h"

void  mx_err_dup_bridges(t_islands *isl) {
     isl->dist = mx_strsplit(isl->string, ' ');
     int i;
     int j;
     for( i = 0; isl->string != '\n'; i++);
          

     
}
